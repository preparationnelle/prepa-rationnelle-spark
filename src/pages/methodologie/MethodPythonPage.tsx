
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function MethodPythonPage() {
  return (
    <div className="container max-w-2xl py-10 animate-fade-in">
      <Card>
        <CardHeader>
          <CardTitle>Python – Méthodologie ECG (bonus)</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-muted-foreground">
            Ce module bonus sur Python pour les prépas ECG sera prochainement disponible ici !
          </p>
        </CardContent>
      </Card>
    </div>
  );
}
